# 植物VCF质量评估指南实施矩阵

本文件对应《总规划》《vcf认识》《补充规划1》《补充规划2》和《植物细化规划》。状态不是营销标签，而是报告运行时口径：`complete` 表示当前输入可计算，`conditional` 表示必须提供额外数据，`not_applicable` 表示不能由 VCF 单独可靠推断。

## 已实现

- 新增独立表型质控工作流：支持XLSX/CSV/TSV/TXT宽表与长表、自动/手动列映射、原始重复均值、环境汇总、描述统计、环境调整BLUE、经验随机效应BLUP和BLUP可靠度。遗传力进一步区分单次观测、环境内材料均值和多年多点材料均值H²，输出遗传、G×E、残差方差、设计连通性/完整度和分环境结果；缺少真实重复时拒绝伪造多年多点H²，复杂空间设计和正式G×E推断明确要求用REML/Cullis模型复核。
- 表型离群值同时比较IQR、3-sigma、MAD robust-z、双侧尾部分位数和生物学区间，使用者可设置共识数；离群候选只导出复核，不自动删除。同步检查缺失、非数值、重复键、零方差、小样本、年份/地点偏移、整体时间趋势和地点趋势分化。
- 表型图形支持全部年份合并、逐年独立或年度多曲线密度图，动态HTML可播放时间变化和经纬度地理分布；同时输出Pearson/Spearman表型相关表与相关热图。
- 棉花表型预设区分陆地棉、海岛棉和草棉；对指南中有依据的18个性状设置单株/群体均值复核区间和置信度。数据不足或定义不明性状不设置伪精确阈值，百分比0–1编码会先识别尺度再比较。
- 分析定位收紧为植物VCF：内置Profile仅覆盖植物、育种材料、多倍体、棉花和植物单倍体/细胞器；动物仅允许走自定义入口，且必须明确物种、GT倍性、繁殖方式、12项核心阈值和责任确认，后端同步校验。
- 新增常见作物选择接口：水稻；普通/面包小麦、硬粒小麦、野生二粒小麦和一粒小麦；陆地棉、海岛棉和草棉；以及大豆、玉米、谷子/高粱、拟南芥、番茄、栽培马铃薯、辣椒、瓜类和栽培烟草。小麦与棉花预设分别记录物种、倍性和亚基因组差异；选择后自动填入物种、参考版本提示、建议基因组大小、倍性、GT编码倍性、亚基因组、繁殖方式和完整阈值起始集。
- 作物预设不锁定参数：使用者可以继续修改所有上下文和阈值；后端比较最终值与预设值，在HTML、JSON和运行清单中分别标记“作物预设”与“用户自定义（基于某作物）”。
- QUAL建议线只在字段覆盖率≥80%时进入智能质控；Ti/Tv经验线只在至少1000条双等位SNP时产生解释性提醒，两者均不会脱离caller、参考版本或变异类型被机械解释。
- Header、reference/source/FILTER/INFO/FORMAT/contig 审计，排序、畸形记录、相邻重复和 INDEL 最简表达检查。
- SNP、INDEL、MNP、SVTYPE/BND/CNV 分类，FILTER、Ti/Tv、MAF、位点缺失率和 1 Mb 密度热点。
- QUAL、QD、MQ、FS、SOR、MQRankSum、ReadPosRankSum 的覆盖率和分位数。
- 样本 call rate、缺失率、DP、GQ、AB、杂合率、GT 倍性和相位率。
- 基于规范化双等位、缺失率/MAF过滤面板的 HWE、PCA、IBD/PI_HAT、IBS、基因型不一致率、差异分、最近邻、近交系数F、LD衰减和ROH；PCA/亲缘使用LD剪枝面板，LD衰减使用未剪枝QC面板。
- 新增独立的多源样本相似性与亲缘页：SNP/INDEL/SV VCF可单独或任意组合，分别计算后按可比较位点数整合IBS、IBS0、PI_HAT和KING-robust；支持导入PLINK 2/KING `.kin0`，并输出每类明细、综合样本对、最近邻、复核候选、IBS/KING方阵、热图、关系图、HTML和ZIP。外部KING与VCF计算KING并存时输出一致性，输入缺少IBS列时不从亲缘系数伪造IBS。
- PCA稳健距离离群、样本相似度摘要、亲缘连通分量，并将适用的样本对/HWE证据保守合入总报告。
- 可选FASTA+.fai的REF/contig长度核验、样本Group/Batch元数据分层、BED/BED.GZ区域重叠、ANN/CSQ/BCSQ注释覆盖与后果统计。
- 棉花/多倍体A/D亚基因组统计及结合杂合率、等位平衡和深度的疑似假杂合窗口。
- SV 长度、IMPRECISE、CIPOS/CIEND 和支持证据字段覆盖率。
- HTML、JSON、TSV、ZIP、运行清单、告警表、推荐过滤表；HTML可打印/另存PDF。
- bcftools 两阶段安全执行器：CSI索引、排序副本、标准化/拆分副本、统计标签副本、精确去重、样本子集、条件掩蔽GT、表达式过滤副本；永不覆盖源文件或已有目标，并生成修复前后stats与JSON清单。
- 物种/VCF自适应质控：按Profile和实际字段覆盖生成保守默认参数，也允许逐项自定义；质控后自动计算评分、记录/样本变化、告警消长和继续质控建议，输出HTML/JSON对比报告。
- 样本缺失率×杂合率图采用P98稳健主坐标、Profile阈值线与贴边离群点标签，避免极端样本压缩主体分布。
- 报告新增“下游就绪/谨慎/暂停”结论、前5项优先问题、文件/样本/位点/群体/来源/模块分层评分、固定章节导航、告警级别筛选、样本搜索与按实际模块生成的下载入口。
- 主页面新增“载入→评估→质控复评→查询/关联解释”状态工作流；质量告警样本可一键带入样本统计，存在位点可一键带入基因型、矩阵或Lead分析。
- 新增 `analysis_priorities.tsv` 与 `score_dimensions.tsv`，并在运行清单中固定规则集版本、下游就绪结论及输入指纹口径；关系和ROH模块启用时继续导出 `pairwise_similarity.tsv` 与 `roh_segments.tsv`。

## 有额外输入时可用

- GFF3/GTF 基因位置、基因结构轨道；ANN/CSQ/BCSQ 或外部表的功能后果；蛋白结构域；GWAS `.ps` 与表型联动：在 Lead 位点高级分析页使用。
- LDBlockShow 正式 block、R²/D′ 区域图：安装后在 Lead 位点高级分析页使用。
- FASTA、样本表、BED 已接入专用输入框；未提供时运行时状态为 `conditional`，提供后输出独立审计结果与表格。

## 不由 VCF 单独下结论

- 污染比例、测序/比对质量、CNV read-depth 证据需要 BAM/CRAM 或专用 sidecar。
- 临床致病性、外部人群频率和疾病数据库不属于当前植物聚焦范围；非植物数据即使使用自定义参数，也不提供这类动物/临床默认解释。
- UMAP 只适合辅助浏览，不替代 PCA/亲缘估计，当前不作为质量判定。

## 解释限制

- 所有作物数值均为《植物细化规划》给出的建议起始口径，必须结合测序深度、变异caller、材料世代、群体设计、参考基因组版本和研究目的复核。
- 棉花、自交系和多倍体的 HWE 默认仅描述；杂合率优先使用队列稳健离群和材料类型解释。
- PLINK 1.9 输出的是 IBD/PI_HAT 与 IBS DST，不冒充 KING kinship。高相似样本对是复核证据，不自动删除样本。
- 独立样本相似性页只有在PLINK 2或外部`.kin0`提供证据时才报告KING-robust；人类二倍体KING分界线对植物仅作相对复核提示，不能直接解释为植物谱系级别。
- 智能扫描时总记录、类型、FILTER、排序和密度使用全文件；基因型和高成本分布使用跨全基因组确定性抽样。
